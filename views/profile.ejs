<!DOCTYPE html>
<html lang="tr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= profileUser.username %> - Profil - AniMax</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        :root {
            --primary: #3b82f6;
            --secondary: #ec4899;
            --dark: #0f172a;
            --light: #f8fafc;
            --glass: rgba(15, 23, 42, 0.9);
            --glass-hover: rgba(15, 23, 42, 0.8);
            --border: rgba(255, 255, 255, 0.1);
        }

        body {
            background-color: #1a1b26;
            min-height: 100vh;
            color: var(--light);
        }

        .side-nav {
            width: 280px;
            height: 100vh;
            position: fixed;
            left: 0;
            top: 0;
            background: #13141f;
            border-right: 1px solid rgba(255, 255, 255, 0.1);
            padding: 2rem;
            transition: all 0.3s ease;
            z-index: 40;
        }

        .nav-link {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem;
            color: rgba(255, 255, 255, 0.8);
            border-radius: 8px;
            transition: all 0.3s ease;
            margin-bottom: 0.5rem;
        }

        .nav-link:hover {
            background: rgba(255, 255, 255, 0.1);
            color: var(--light);
        }

        .nav-link.active {
            background: var(--primary);
            color: var(--light);
        }

        .main-content {
            margin-left: 280px;
            padding: 2rem;
            width: calc(100% - 280px);
        }

        .profile-header {
            background: linear-gradient(to bottom, rgba(0,0,0,0.7), rgba(0,0,0,0.3));
            border-radius: 1rem;
            padding: 2rem;
            margin-bottom: 2rem;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .anime-card {
            background: #13141f;
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 0.5rem;
            overflow: hidden;
            transition: all 0.3s ease;
        }

        .anime-card:hover {
            transform: translateY(-5px);
            border-color: var(--primary);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
        }

        .section-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 1rem;
            color: #fff;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .role-badge {
            padding: 4px 8px;
            border-radius: 9999px;
            font-size: 0.75rem;
            display: inline-flex;
            align-items: center;
            gap: 4px;
            margin-right: 4px;
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .role-founder {
            background: linear-gradient(45deg, #ffd700, #ffa500);
            color: #000;
            border: none;
        }

        .role-admin {
            background: linear-gradient(45deg, #3b82f6, #1d4ed8);
            color: white;
        }

        .role-guide {
            background: linear-gradient(45deg, #10b981, #059669);
            color: white;
        }

        .role-contributor {
            background: linear-gradient(45deg, #f472b6, #db2777);
            color: white;
        }

        .role-premium {
            background: linear-gradient(45deg, #9333ea, #7c3aed);
            color: white;
        }

        .role-supporter {
            background: linear-gradient(45deg, #f59e0b, #d97706);
            color: white;
        }

        @media (max-width: 768px) {
            .side-nav {
                width: 100%;
                height: 4rem;
                bottom: 0;
                top: auto;
                flex-direction: row;
                justify-content: space-around;
                padding: 0.5rem;
                border-right: none;
                border-top: 1px solid var(--border);
            }

            .nav-link {
                padding: 0.5rem;
                margin-bottom: 0;
            }

            .nav-link span {
                display: none;
            }

            .nav-link i {
                font-size: 1.2rem;
            }

            .main-content {
                margin-left: 0;
                padding: 1rem;
                width: 100%;
                margin-bottom: 4rem;
            }
        }
    </style>
</head>
<body>
    <%- include('partials/navbar') %>

    <!-- Ana İçerik -->
    <main class="main-content">
        <!-- Profil Başlığı -->
        <div class="profile-header">
            <div class="flex items-center gap-6">
                <div class="relative">
                    <% if (profileUser.discordAvatar) { %>
                        <img src="https://cdn.discordapp.com/avatars/<%= profileUser.discordId %>/<%= profileUser.discordAvatar %>.png" 
                            alt="<%= profileUser.username %>"
                            class="w-24 h-24 rounded-full border-2 border-blue-500">
                    <% } else if (profileUser.avatar) { %>
                        <img src="<%= profileUser.avatar %>" alt="<%= profileUser.username %>"
                            class="w-24 h-24 rounded-full border-2 border-blue-500">
                    <% } else { %>
                        <div class="w-24 h-24 rounded-full border-2 border-blue-500 bg-gray-700 flex items-center justify-center">
                            <i class="fas fa-user text-3xl text-gray-400"></i>
                        </div>
                    <% } %>
                    <% 
                    const founderIds = ['1246506868977696811', '1245436966972031069', '1112945015132536943'];
                    if (founderIds.includes(profileUser.discordId)) { 
                    %>
                        <div class="absolute -top-1 -right-1 bg-gradient-to-r from-yellow-400 to-yellow-600 rounded-full p-1.5 border-2 border-white">
                            <i class="fas fa-crown text-white text-sm"></i>
                        </div>
                    <% } %>
                </div>
                <div>
                    <div class="flex items-center gap-3 mb-2">
                        <h1 class="text-2xl font-bold"><%= profileUser.username %></h1>
                    </div>
                    <div class="flex flex-wrap gap-2 mb-3">
                        <% if (founderIds.includes(profileUser.discordId)) { %>
                            <span class="role-badge role-founder">
                                <i class="fas fa-crown"></i>
                                Kurucu
                            </span>
                        <% } %>
                        
                        <% if (profileUser.discordRoles) { %>
                            <% if (profileUser.discordRoles.includes('1330993157361500303')) { %>
                                <span class="role-badge role-admin">
                                    <i class="fas fa-shield-alt"></i>
                                    Admin
                                </span>
                            <% } %>
                            
                            <% if (profileUser.discordRoles.includes('1331976305264300042')) { %>
                                <span class="role-badge role-guide">
                                    <i class="fas fa-book"></i>
                                    Rehber
                                </span>
                            <% } %>
                            
                            <% if (profileUser.discordRoles.includes('1331976244069273661')) { %>
                                <span class="role-badge role-contributor">
                                    <i class="fas fa-share-alt"></i>
                                    Paylaşımcı
                                </span>
                            <% } %>
                            
                            <% if (profileUser.discordRoles.includes('1332440779309973525')) { %>
                                <span class="role-badge role-premium">
                                    <i class="fas fa-gem"></i>
                                    Premium
                                </span>
                            <% } %>
                            
                            <% if (profileUser.discordRoles.includes('1332379048407470180')) { %>
                                <span class="role-badge role-supporter">
                                    <i class="fas fa-heart"></i>
                                    Destekçi
                                </span>
                            <% } %>
                        <% } %>
                    </div>
                    <p class="text-gray-400 text-sm">
                        <i class="fas fa-calendar-alt mr-2"></i>
                        <%= new Date(profileUser.createdAt).toLocaleDateString('tr-TR') %>
                    </p>
                </div>
            </div>
        </div>

        <!-- Animeler -->
        <div class="section-title">
            <i class="fas fa-heart text-red-500"></i>
            Animeler
        </div>
        <% if (profileUser.favorites && profileUser.favorites.length > 0) { %>
            <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 lg:grid-cols-5 gap-4">
                <% profileUser.favorites.forEach(anime => { %>
                    <a href="/anime/<%= anime._id %>" class="anime-card">
                        <div class="relative aspect-[2/3]">
                            <img src="<%= anime.imageUrl %>" alt="<%= anime.name %>" 
                                class="w-full h-full object-cover">
                            <div class="absolute bottom-0 left-0 right-0 p-2 bg-gradient-to-t from-black/90 to-transparent">
                                <h3 class="text-sm font-medium text-white line-clamp-2"><%= anime.name %></h3>
                            </div>
                        </div>
                    </a>
                <% }); %>
            </div>
        <% } else { %>
            <div class="text-center py-8 bg-[#13141f] rounded-lg border border-gray-800">
                <i class="fas fa-heart-broken text-3xl text-gray-600 mb-3"></i>
                <p class="text-gray-400 text-sm">Henüz favori anime eklenmemiş.</p>
            </div>
        <% } %>
    </main>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/flowbite/2.2.1/flowbite.min.js"></script>
</body>
</html> 